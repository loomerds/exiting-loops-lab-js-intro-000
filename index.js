function breakOut(array, changeValue, stopValue) {
  array.forEach(element=>{
    if(element===stopValue) {break;}
    else(element = changeValue);
  });
}

function keepGoing(array, changeValue, skipValue) {

}

function findBy(array, findFn) {

}
